providers = ["node"]

[phases.setup]
only_include_files = ["package.json"]

[phases.install]
only_include_files = ["package.json", "package-lock.json"]

[phases.build]
cmds = ["npx prisma generate"]

[phases.start]
cmd = "node dist/index.js"
